<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title></title>
	</head>
	<body>
		<header>
			<h1>Edit <%=attraction.title%>'s Info</h1>
			<nav>
				<ul>
					<li>
						<a href="/">Home</a>
					</li>
					<li>
						<a href="/attractions">Attractions Index</a>
					</li>
				</ul>
			</nav>
		</header>
		<main>
			<h2>Attraction Attributes:</h2>
			<form action="/attractions/<%=attraction._id%>?_method=PUT" method="post">
				<select name="authorId">
                    <% for(var i = 0; i < areas.length; i++) { %>
                        <option
							value="<%=areas[i]._id%>"
							<% if(areas[i]._id.toString() === areaAttraction._id.toString()){ %>
								selected
							<% } %>
							>
							<%=areas[i].name%>
						</option>
                    <% } %>
                </select><br/>
				<input type="text" name="title" value="<%=attraction.title%>"/><br/>
				<textarea name="body"><%=attraction.body%></textarea><br/>
				<input type="submit" value="Update Attraction"/>
			</form>
		</main>
	</body>
</html>
